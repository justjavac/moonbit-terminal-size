name = "justjavac/terminal_size/examples/show_size"

version = "0.1.0"

import {
  "justjavac/terminal_size@0.1.5",
}

repository = "https://github.com/justjavac/moonbit-terminal-size/tree/main/examples/show_size"

license = "MIT"

keywords = [ "terminal-size", "native", "example" ]

description = "Example for justjavac/terminal_size."

preferred_target = "native"

supported_targets = "+native"

options(
  source: ".",
)
